/**
 * Route entry for the GM console. The screen itself lives in `./home/` beside
 * its data layer and the party roster it now leads with; this file stays so
 * `router.tsx` keeps its existing import path (same pattern as `BooksPage`).
 */
export { default } from './home/GmHome.js';
