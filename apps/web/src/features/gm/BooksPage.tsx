/**
 * Route entry for the rules library (M11). The screen itself lives with the
 * rest of the books feature in `./books/`, next to the calibration logic and
 * the ref chips it shares code with; this file stays so `router.tsx` keeps its
 * existing import path.
 */
export { default } from './books/BooksPage.js';
